version: 2

models:
 - name: bronze_products
   columns:
    - name: category
      tests: 
       - accepted_values: 
           values: ["Doohickey", "Widget", "Gizmo","Gadget"]
           #config:
           #       severity: warn

    - name: id
      tests: 
       # Specify FOREIGN KEY COLUMN to another DBT Data Model (in this case bronze_orders)
       - relationships:
          field: product_id
          to: ref('bronze_orders')